# Desktop Notification Cronjobs

These cronjobs were developed on Ubuntu 16.04, and use the notify-send tool to provide desktop notifications from cronjobs

Requirements:

1. lm-sensors (sudo apt install lm-sensors)
